pipeline {
    agent any

    stages {

   stage('Git Clone') {
            steps {
                sh 'rm -Rf proj-feb'
                sh 'git clone https://github.com/Sudan15423/jenki.git'
            }
        }           
    }
}

